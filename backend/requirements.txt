# SDK de Google Gemini (motor gratuito de la recepcionista)
google-genai>=1.0.0

# SDK oficial de Anthropic (Claude) — asistente de investigación jurídica
anthropic>=0.69.0

# Servidor web para exponer el agente y el webhook de mensajería
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Cliente HTTP para llamar a la Graph API de Meta (WhatsApp/Messenger/Instagram)
httpx>=0.27.0

# Carga de variables de entorno desde .env
python-dotenv>=1.0.0
